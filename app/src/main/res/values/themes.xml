<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyPartyApp" parent="Theme.SplashScreen">
        <!-- Splash: малиновый логотип на белом фоне (надёжно виден на всех телефонах) -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.MyPartyApp.App</item>
    </style>

    <style name="Theme.MyPartyApp.App" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
